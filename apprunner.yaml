version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - pip install -r requirements.txt
run:
  command: python application.py
  env:
   - name : FAMILA
     value: "Qarzhau"
   - name : NAME
     value: "Ramazan"
